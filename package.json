{
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5"
  },
  "scripts": {
    "build": "tsc && ncc build dist/main.js -o dist/bundle",
    "typecheck": "tsc --noEmit"
  }
}