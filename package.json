{
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.0",
    "@actions/github": "^6.0.0",
    "semver": "^7.6.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/semver": "^7.5.8",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5",
    "vitest": "^3.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && ncc build dist/main.js -o dist/bundle",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}